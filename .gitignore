# ----------------------------------------------------------------------
# 1. DEPENDENCY DIRECTORIES
# ----------------------------------------------------------------------

# Node Package Manager (npm)
# The `node_modules` directory should always be ignored as it contains 
# easily reproducible dependency files.
node_modules
/backend/node_modules
/client/node_modules


# ----------------------------------------------------------------------
# 2. BACKEND (NODE / EXPRESS) SPECIFIC
# ----------------------------------------------------------------------

# Compiled/Transpiled JavaScript files
# Often used for TypeScript or Babel builds
/dist
/build
/out

# Runtime data files
# Log files, databases (unless version control is required for SQLite, etc.)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.pnp.*

# Environment variables
# Crucial for security; DO NOT commit these files.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local


# ----------------------------------------------------------------------
# 3. FRONTEND (REACT / CRA / Vite) SPECIFIC
# ----------------------------------------------------------------------

# Create React App build output
# (If using Create React App or a similar tool)
/client/build
/client/dist

# Vite/Next.js/etc. specific build and cache files
.vite
.next
.cache

# Storybook or other UI documentation
storybook-static

# ----------------------------------------------------------------------
# 4. OPERATING SYSTEM / EDITOR / IDE FILES
# ----------------------------------------------------------------------

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history

# JetBrains IDEs (WebStorm, IntelliJ)
.idea/